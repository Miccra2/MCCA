@import io

i32 main(i32 argc, *char[] argv) {
    $io.print("hello from M-lang!");       // use strings
    $io.print("argc: {argc}");             // format strings
    $io.print("'\{\}': \\\{\\\}");         // escape strings
    return 0;
}

@build {
    @entry = main;
}
